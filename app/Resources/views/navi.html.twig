{% trans_default_domain 'AppBundle' %}
<!-- Fixed navbar -->
			    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
			    	<div class="container">
			        	<div class="navbar-header">
							<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
								<span class="sr-only">Toggle navigation</span>
								<span class="icon-bar"></span>
								<span class="icon-bar"></span>
								<span class="icon-bar"></span>
							</button>
							<a class="navbar-brand" href="homepage">RichJanitor Watchfloor Admin</a>
						</div>
	
						<div class="navbar-collapse collapse">
							<ul class="nav navbar-nav navbar-right">
								<li class="{% if app.request.attributes.get('_route') == 'homepage' %}active{% endif %}"><a href="{{ path('homepage') }}">Home</a></li>

								<li class="dropdown{% if (app.request.attributes.get('_route') starts with 'account') or (app.request.attributes.get('_route') == 'fos_user_security_login') %} active{% endif %}">
									<a href="#" class="dropdown-toggle" data-toggle="dropdown">Account<span class="caret"></span></a>
									
									<ul class="dropdown-menu multi-level" role="menu">
										{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}{# Show the logout if not logged in, else show the sign up and register #}
											<li><a href="{{ path('fos_user_change_password') }}">Change Password</a></li>
											<li class="divider"></li>
											<li><a href="{{ path('fos_user_security_logout') }}">Log Out</a></li>
										{% else %}{# User isnt logged in. Show sign-in and register #}
											<li><a href="{{ path('fos_user_security_login') }}">{{'menu.signin'|trans}}</a></li>
											<li><a href="{{ path('fos_user_registration_register') }}">{{'menu.create_account'|trans}}</a></li>
										{% endif %}
									</ul>
								</li>

								<li class="dropdown{% if (app.request.attributes.get('_route') starts with 'product')%} active{%endif%}">
									<a href="#" class="dropdown-toggle" data-toggle="dropdown">Invoices<span class="caret"></span></a>
									<ul class="dropdown-menu" role="menu">
										<li><a href="{{ path('admin_invoices') }}">Invoices</a></li>
									</ul>
	                            </li>
							
							</ul>
						</div><!--/.nav-collapse -->
					</div>
			    </div>
			    <!-- End Fixed navbar -->